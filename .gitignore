# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
.venv*/
venv/
env/

# IDE
.idea/
.vscode/
*.iml

# macOS
.DS_Store

# ── Pipeline runtime outputs ──────────────────────────────────────────────────
runs/
pipeline_runs.sqlite
pipeline_runs.sqlite-shm
pipeline_runs.sqlite-wal
preprocessing_run.json
app.log
*.log

# Intermediate alignment / QC outputs written by the pipeline nodes (bulk)
01_qc/
02_trimmed/
03_bam/
04_counts/
05_normalized/

# scRNA-seq step outputs written to the project root during a run
00_load/
02_filter/
03_normalize/
04_pca/
05_cluster/
06_markers/
07_trajectory/
09_benchmark/

# Jupyter
.ipynb_checkpoints/

# ── Large model / embedding files ─────────────────────────────────────────────
# Pre-trained model weights downloaded at runtime
biobert_v1.1_pubmed_pytorch_model/
*.ckpt

# ── Data directory ────────────────────────────────────────────────────────────
# Binary data files (PPI graphs, ESM embeddings, pathway embeddings, RAG index)
# are too large for git.  See data/README.md for download instructions.

# Exclude everything in data/ except the README
data/*
!data/README.md

# Genome reference and STAR index (large, not committed)
data/genome/
data/star_index/
.env
.env
